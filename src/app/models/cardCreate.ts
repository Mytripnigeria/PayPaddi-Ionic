export interface ICardCreate {
  currency: string;
  amount: number;
  debit_currency: string;
  name: string;
  address: string;
  city: string;
  state: string;
  postal_code: string;
  country: string;
}
